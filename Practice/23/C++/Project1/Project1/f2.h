#ifndef KE_2
#define KE_2
#include "f1.h"
#include <iostream>

double sin(int i, int x) {
	int i; int x;
	double f = 0;
	for (int i = 0; i < n; i++) {
		res += pow(-1, i) * (pow(x, (2 * i + 1)) / Factorial(2 * i + 1));
	}
	return f;
#endif KE_2
